map $http_connection $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    server_name _;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log warn;

    client_max_body_size 50M;
    client_body_timeout 60s;
    send_timeout 60s;

    location = / {
        return 200 'NGINX gateway is running';
        add_header Content-Type text/plain;
    }

    # =======================
    # IDENTITY SERVICE
    # =======================
    location ^~ /api/v1/companies {
        proxy_pass http://host.docker.internal:3001/api/v1/companies;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    location ^~ /api/v1/users {
        proxy_pass http://host.docker.internal:3001/api/v1/users;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # =======================
    # ATTENDANCE SERVICE
    # =======================
    # location /api/v1/attendance/ {
    #     proxy_pass http://service_attendance:8080/api/v1/attendance/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # =======================
    # WORKFORCE SERVICE
    # =======================
    location ^~ /api/v1/shift {
        proxy_pass http://host.docker.internal:8081/api/v1/shift;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location ^~ /api/v1/employee/shift {
        proxy_pass http://host.docker.internal:8081/api/v1/employee/shift;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }


    # =======================
    # AUTH SERVICE
    # =======================
    location /api/v1/auth/ {
        proxy_pass http://host.docker.internal:8080/api/v1/auth/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

    }

    # =======================
    # DEVICE SERVICE
    # =======================
    # location /api/v1/device/ {
    #     proxy_pass http://service_device:8080/api/v1/device/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # location /api/v1/device {
    #     proxy_pass http://service_device:8080/api/v1/device;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # =======================
    # SWAGGER (route về đúng service)
    # =======================
    # location /swagger/auth/ {
    #     proxy_pass http://service_auth:8080/swagger/;
    # }

    # location /swagger/device/ {
    #     proxy_pass http://service_device:8080/swagger/;
    # }

    # location /swagger/workforce/ {
    #     proxy_pass http://host.docker.internal:8080/swagger/;
    # }

    # location /swagger/attendance/ {
    #     proxy_pass http://service_attendance:8080/swagger/;
    # }

    location / {
        return 404 'Not found on gateway';
        add_header Content-Type text/plain;
    }
}