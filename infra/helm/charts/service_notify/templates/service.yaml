apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_notify.fullname" . }}
  labels:
    {{- include "service_notify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_notify.selectorLabels" . | nindent 4 }}
