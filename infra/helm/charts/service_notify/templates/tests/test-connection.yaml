apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service_notify.fullname" . }}-test-connection"
  labels:
    {{- include "service_notify.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service_notify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
