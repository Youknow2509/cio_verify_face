apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service_workforce.fullname" . }}-test-connection"
  labels:
    {{- include "service_workforce.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service_workforce.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
