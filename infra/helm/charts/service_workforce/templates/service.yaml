apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_workforce.fullname" . }}
  labels:
    {{- include "service_workforce.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_workforce.selectorLabels" . | nindent 4 }}
