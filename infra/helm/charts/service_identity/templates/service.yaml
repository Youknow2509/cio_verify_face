apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_identity.fullname" . }}
  labels:
    {{- include "service_identity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_identity.selectorLabels" . | nindent 4 }}
