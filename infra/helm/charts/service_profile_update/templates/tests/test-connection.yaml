apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service_profile_update.fullname" . }}-test-connection"
  labels:
    {{- include "service_profile_update.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service_profile_update.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
