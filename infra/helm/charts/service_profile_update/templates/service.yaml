apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_profile_update.fullname" . }}
  labels:
    {{- include "service_profile_update.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_profile_update.selectorLabels" . | nindent 4 }}
