apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_device.fullname" . }}
  labels:
    {{- include "service_device.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_device.selectorLabels" . | nindent 4 }}
