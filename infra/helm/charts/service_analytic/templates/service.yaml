apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_analytic.fullname" . }}
  labels:
    {{- include "service_analytic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_analytic.selectorLabels" . | nindent 4 }}
